
<% @page_title = "#{@document.name} | Pages" %>


<%= link_to("<< Back to List", documents_path, :class => 'back-link') %> <br>

 <%= @document.name %> <br>

 <% @pages.each do |page| %>

 <%= page.position %>    <!-- TODO: fix grid style -->
 <%= link_to(image_tag("/documents/document #{@document.id}/imgs/#{page.position}.jpg",size: "200x200"), document_page_path(document_id: params[:document_id] , id: page.id)) %>
<% end %>
